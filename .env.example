# Xray API Configuration for Data Center/Server
# Copy this file to .env and fill in your actual values

# Base URL for your Xray Data Center/Server instance (required)
# Example: https://your-xray-server.company.com or http://xray-server:8080
XRAY_URL=https://your-xray-server.company.com

# Personal Access Token for Xray Data Center/Server (required)
# Generate this in Xray under Administration > User Management > Personal Access Tokens
# Or in your user profile settings
XRAY_PERSONAL_TOKEN=your-personal-access-token-here

# Path to OpenAPI specification file or URL (required)
# Can be a local file path or a URL to the OpenAPI spec
# For Data Center/Server, typically a local file path is used
# Examples:
#   - Local file: ./xray_v2.0.json (provided in this repository)
#   - Absolute path: /path/to/xray_openapi_spec.json
#   - Remote URL: https://your-xray-server.company.com/api/openapi.json
XRAY_OPENAPI_SPEC=./xray_v2.0.json

# Optional: Xray client timeout in seconds (default: 20.0)
# XRAY_TIMEOUT=20.0

# Optional: Path to MCP names mapping file (default: mcp_names_mapping.json)
# This file maps MCP names to their corresponding IDs or other identifiers
# If not specified, the default file in the repository will be used
# Example: ./mcp_names_mapping.json
XRAY_MCP_NAMES_FILE=mcp_names_mapping.json

# Optional: Read-only mode (default: False)
# If set to True, the server will not allow any modifications to Xray data
# Useful for testing or read-only operations
XRAY_READ_ONLY=False
